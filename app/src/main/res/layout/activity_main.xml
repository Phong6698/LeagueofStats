<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    android:clickable="true"
    android:background="#fff0f0f0"
    android:id="@+id/relative">


    <ImageView
        android:layout_width="fill_parent"
        android:layout_height="150dp"
        android:id="@+id/imageView"
        android:src="@drawable/ls_icon"
        android:layout_alignParentStart="true"
        android:layout_marginTop="10dp" />

    <Button
        android:layout_width="250dp"
        android:layout_height="60dp"
        android:text="@string/freetoplay_btn"
        android:id="@+id/free_to_play_btn"
        android:background="@android:color/holo_blue_dark"
        android:textColor="#ffffffff"
        android:clickable="true"
        android:layout_below="@+id/imageView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="25dp" />

    <Button
        android:layout_width="250dp"
        android:layout_height="60dp"
        android:text="@string/champions_btn"
        android:id="@+id/champions_btn"
        android:background="@android:color/holo_blue_dark"
        android:textColor="#ffffffff"
        android:layout_below="@+id/free_to_play_btn"
        android:layout_alignStart="@+id/items_btn"
        android:layout_marginTop="10dp" />

    <Button
        android:layout_width="250dp"
        android:layout_height="60dp"
        android:text="@string/items_btn"
        android:id="@+id/items_btn"
        android:background="@android:color/holo_blue_dark"
        android:textColor="#ffffffff"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/champions_btn"
        android:layout_marginTop="10dp" />

    <Button
        android:layout_width="250dp"
        android:layout_height="60dp"
        android:text="@string/favoritSummoners_btn"
        android:id="@+id/favoritSummoners_btn"
        android:background="@android:color/holo_blue_dark"
        android:textColor="#ffffffff"
        android:layout_below="@+id/items_btn"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp" />

</RelativeLayout>